package com.sjocol.cleanservices.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta con mejor contraste
val BrandBlue = Color(0xFF1E5A8A)      // Primario más profundo (mejor AA)
val BrandTeal = Color(0xFF0FA3B1)      // Secundario con mayor saturación
val BackgroundLight = Color(0xFFF5F7FA) // Fondo claro
val TextPrimary = Color(0xFF1F2933)    // Texto principal más oscuro
val DividerLight = Color(0xFFD5DBE1)   // Bordes/Divisores

// Derivados/soportes
val SurfaceVariantLight = Color(0xFFE9EEF3) // superficies elevadas sutiles

// Modo oscuro con mejor contraste
val BackgroundDark = Color(0xFF0E1418)
val TextOnDark = Color(0xFFEDF2F7)
val DividerDark = Color(0xFF22303A)
val SurfaceVariantDark = Color(0xFF12202A)